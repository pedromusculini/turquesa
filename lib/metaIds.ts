/**
 * IDs oficiais Meta — Turquesa Agenda.
 * Dataset/Pixel, Página e Instagram confirmados no Gerenciador de Eventos (set/2026).
 */

/** Conjunto de dados / Pixel (Events Manager). */
export const META_DATASET_ID = '2631118973956450';
export const META_PIXEL_ID_DEFAULT = META_DATASET_ID;

/** Conta de anúncios (prefixo act_). */
export const META_AD_ACCOUNT_ID = 'act_977732793417355';

/** Portfólio Business Manager. */
export const META_BUSINESS_ID = '27093548660317256';

/** Página Facebook usada nos anúncios (não usar a Página “turquesaagenda”). */
export const META_FACEBOOK_PAGE_ID = '1158752407323598';

/** Página antiga / handle turquesaagenda — não conectar a anúncios nem ao dataset. */
export const META_FACEBOOK_PAGE_ID_LEGACY = '1290270830831023';

/**
 * Instagram profissional ligado ao dataset no assistente de eventos.
 * (ID que a Meta mostrou ao conectar o conjunto de dados.)
 */
export const META_INSTAGRAM_DATASET_ID = '17841432964039694';

/**
 * Instagram Graph API (Reels / npm run meta:ig:accounts).
 * Pode diferir do ID do dataset — a Graph usa o user id da conta IG.
 */
export const META_INSTAGRAM_GRAPH_USER_ID = '28497523596551273';

/** App Cursor-IG (publicação orgânica), não é o Pixel. */
export const META_APP_ID_INSTAGRAM = '1566341235217456';
